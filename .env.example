# Copy to .env for local development. Never commit .env.

# Path to the SQLite database file.
# On Railway: set this to a volume-mounted path (e.g. /data/employees.db)
# to persist data across deploys. Omit to use ./employees.db locally.
HR_DB_PATH=./employees.db

# API key for the HR service. Required in production.
# Generate with: python -c "import secrets; print(secrets.token_urlsafe(32))"
HR_API_KEY=
