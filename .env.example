# Copy to .env for local development. Never commit .env.

# Postgres connection URL.
# On Railway this is injected automatically via the ${{Postgres.DATABASE_URL}} reference.
# For local dev, spin up a local Postgres and set the URL here.
DATABASE_URL=postgresql://user:password@localhost:5432/employeehr

# API key for the HR service. Required in production.
# Generate with: python -c "import secrets; print(secrets.token_urlsafe(32))"
HR_API_KEY=
